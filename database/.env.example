# Configuración de Conexión PostgreSQL
# Archivo de variables de entorno para desarrollo local

# Información del servidor PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_password_aqui
POSTGRES_DB=clientpro_crm

# URL de conexión completa (usada por Prisma y otros ORMs)
DATABASE_URL=postgresql://postgres:tu_password_aqui@localhost:5432/clientpro_crm?schema=public

# Configuración para producción (cambiar cuando despliegues)
# DATABASE_URL=postgresql://usuario:password@host:5432/clientpro_crm?schema=public

# ============================================
# INSTRUCCIONES
# ============================================
# 1. Renombra este archivo a .env (elimina el .example)
# 2. Cambia "tu_password_aqui" por tu contraseña real de PostgreSQL
# 3. NO subas el archivo .env a GitHub (ya está en .gitignore)
